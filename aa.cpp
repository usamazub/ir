// Input : N
// Output : N tipe orang

vector<tipe> solve(int N) {
    vector<tipe> tipe_orang;

    for (int i = 1; i <= N, i++) {
        freq['ksatria'] = freq['rakyat biasa'] = freq['penjahat'] = 0;

        for (int j = 1; i <= N; i++) {
            if (i == j) {
                continue;
            }
            tipe = ask()
        }
    }

    
}
